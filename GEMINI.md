# GEMINI.md

`CLAUDE.md` 의 축약판이다. 둘이 어긋나면 `CLAUDE.md` 가 이긴다.
**현재 상태와 실측 수치는 `STATUS.md` 에 있다. 세션 시작 시 먼저 읽는다.**
작업 절차 자체는 `.claude/skills/room-3d/SKILL.md` 에 있다.

## 계산 위치
- 복원을 이 기계에서 시도하지 않는다. CUDA GPU 가 없다. 코랩 T4 를 쓴다.
- Windows 다. Colab CLI 는 지원하지 않으니 깔지 않는다. Colab MCP 로 간다.

## 코랩 MCP
- `.mcp.json` 은 반드시 `tools/colab_mcp_shim.py` 를 거친다. 직접 등록하면
  셀 조작 도구가 안 열린다 (까닭은 `STATUS.md`).
- 중계기는 `tools/colab_mcp_launch.py` 를 uv 도구 가상환경 파이썬으로 실행한다.
  실행기가 붙을 노트를 고정한다. 바꾸려면 `COLAB_MCP_NOTEBOOK` 환경변수.
- `uvx git+...` 로 등록하지 않는다. `uv tool install` 로 깔아 둔 것을 쓴다.
- **크롬을 먼저 띄운 뒤 Claude Code 를 시작한다.** 꺼져 있으면 연결이 60초를 넘겨 실패한다.
- 오래 걸리는 셀은 백그라운드로 걸고 상태만 확인한다. 도구 호출에 시간 제한이 있다.
- 런타임을 T4 로 바꾸는 것은 사용자만 할 수 있다. 고정 노트면 한 번 고르면 유지된다.

## 복원
- **`amp_dtype='fp16'` 필수.** 기본값 `bf16` 은 T4 에 없다. 작게 시험하면 통과하고
  본 실행에서 터진다.
- 사진은 길이와 무관하게 24장. 고정 fps 금지.
- 각 축 0.5~99.5 백분위 밖은 버린다.
- 카메라 퍼짐 / 장면 대각선이 0.1 미만이면 다시 찍게 한다.

## 알갱이 파일
- `f_rest` 를 헤더에 넣지 않는다. 알갱이당 68바이트를 지킨다.
- 색 `img_no_norm` 은 0~1 이다. 255 로 나누지 않는다.
- y, z 부호를 뒤집는다.
- 반지름은 `K * 가장 긴 변 / sqrt(N)`. 이웃 거리 기준 금지.

## 띄우기
- `SparkRenderer` 를 장면에 넣는다. 안 넣으면 오류 없이 까맣다.
- `.sog` 의 안쪽 이름은 `pcsogszip`.
- 크기는 `getBoundingBox()` 로 잰다. `Box3.setFromObject` 는 빈 상자를 준다.
- Spark 판을 함부로 올리지 않는다.

## 되돌리기 어려운 일
- 공개 저장소 생성·Pages 켜기는 **먼저 확인받는다.** 찍은 공간이 그대로 공개된다.
- 검증용 합성 파일을 커밋하지 않는다.
- 제출 전 `주소/scene.sog` 가 직접 내려오는지 확인한다.

## 문서
코드를 고쳤으면 같은 작업 안에서 맞춘다. 상태·수치는 `STATUS.md`, 규칙은 `CLAUDE.md`.
수치에는 잰 날짜를 적는다. 비밀값은 어디에도 쓰지 않는다.

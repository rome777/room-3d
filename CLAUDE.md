# 작업 규칙

영상 하나에서 3차원 장면을 만들어 웹에 올리는 저장소다.
**현재 상태·실측 수치·사건 기록은 `STATUS.md` 에만 둔다. 이 파일에 쓰지 않는다.**
세션을 시작하면 `STATUS.md` 를 가장 먼저 읽는다.

작업 절차 자체는 `.claude/skills/room-3d/SKILL.md` 에 있다. 이 파일은 그 위에
이 기계와 이 도구 조합에서만 해당하는 규칙을 얹는다.

## 어디서 계산하나

- **복원(영상 → 점)을 이 기계에서 시도하지 않는다.** CUDA GPU 가 없다.
  반드시 코랩 T4 에서 돌린다.
- Windows 이므로 Colab CLI 를 깔지 않는다. 지원하지 않는다. Colab MCP 로 간다.
- 메시·후처리처럼 CPU 를 쓰는 걸음은 이 기계가 더 빠르다.

## 코랩을 MCP 로 다룰 때

- `.mcp.json` 은 **반드시 `tools/colab_mcp_shim.py` 를 거친다.** colab-mcp 를
  직접 등록하면 셀 조작 도구가 안 열린다. 까닭은 `STATUS.md` 에 있다.
- 중계기는 `colab-mcp.exe` 를 직접 부르지 않고 **`tools/colab_mcp_launch.py` 를 uv 도구
  가상환경의 파이썬으로 실행한다.** 실행기가 붙을 노트를 고정한다. 노트를 바꾸려면
  `COLAB_MCP_NOTEBOOK` 환경변수를 준다.
- `uvx git+...` 로 등록하지 않는다. `uv tool install` 로 깔아 둔 것을 쓴다.
- **크롬을 먼저 띄운 뒤 Claude Code 를 시작한다.** 꺼져 있으면 브라우저 찬 시작이
  자식 안쪽 대기 60초를 넘겨 연결이 실패한다.
- **오래 걸리는 셀은 백그라운드 스레드로 걸고 상태만 따로 확인한다.**
  MCP 도구 호출에 시간 제한이 있어 긴 셀은 그대로 두면 잘린다.
- 런타임 유형을 T4 로 바꾸는 것은 대신 해 줄 수 없다. 고정 노트는 Drive 에 저장되어
  한 번 고르면 유지되지만, 새로 잡힌 노트면 사용자에게 부탁한다. 어느 탭인지
  헷갈리면 그 노트에 표시 셀을 넣어 찾게 한다.

## 복원할 때

- **`amp_dtype='fp16'` 을 반드시 넘긴다.** 기본값이 `bf16` 인데 T4 에는 없다.
  작게 시험하면 통과하고 본 실행에서 터지는 종류라 더 위험하다.
- 사진은 영상 길이와 상관없이 **24장**이 되게 뽑는다. 고정 `fps` 를 쓰지 않는다.
- 각 축 0.5~99.5 백분위 밖은 버린다. 안 버리면 장면이 한없이 커진다.
- 카메라 퍼짐 / 장면 대각선이 **0.1 미만이면 다시 찍게 한다.** 그대로 진행하지 않는다.

## 알갱이 파일을 쓸 때

- **`f_rest` 45칸을 헤더에 넣지 않는다.** 칸 17개, 알갱이 하나에 68바이트를 지킨다.
  이 값이 68 이 아니면 헤더가 잘못된 것이다.
- 색은 `img_no_norm` 이 **0~1** 임을 전제한다. 255 로 나누지 않는다.
- y 와 z 의 부호를 뒤집는다. 안 뒤집으면 장면이 거꾸로 선다.
- 알갱이 반지름은 `K * 가장 긴 변 / sqrt(N)`. 이웃 점까지의 거리를 기준으로 삼지 않는다.

## 띄울 때

- `SparkRenderer` 를 장면에 넣는다. 안 넣으면 오류 없이 화면만 까맣다.
- `.sog` 의 Spark 안쪽 이름은 `pcsogszip` 이다.
- `SplatMesh` 크기는 `Box3.setFromObject` 로 못 잰다. `getBoundingBox()` 를 쓴다.
- Spark 판을 함부로 올리지 않는다. 참고 저장소와 같은 판을 유지한다.

## 되돌리기 어려운 일

- **공개 저장소를 만들거나 Pages 를 켜기 전에 반드시 확인받는다.** 찍은 공간이
  그대로 공개되고, 되돌리려면 저장소를 지워야 하며 그 사이 누가 받아 갔는지 알 수 없다.
- 검증용 합성 파일을 커밋하지 않는다. 실제 결과로 덮어쓴 뒤에 커밋한다.
- 제출 전 `주소/scene.sog` 가 직접 내려오는지 확인한다.

## 문서

전역 규칙을 따른다. 코드를 고쳤으면 같은 작업 안에서 문서를 맞춘다.

- 코드·데이터·환경이 바뀜 → `STATUS.md`
- 작업 방식·금지사항이 바뀜 → 이 파일을 고치고 `AGENTS.md`·`GEMINI.md` 에 반영
- 실측 수치에는 잰 날짜를 함께 적는다
- 비밀값은 어디에도 쓰지 않는다

# STATUS

영상 하나에서 3차원 장면을 만들어 웹에 올리는 작업. 모두의연구소
"[프로젝트] 공간을 다루는 AI" 8강 과제 제출이 목표다.

---

# 현재 상태 (2026-09-21 기준)

## 어디까지 왔나

영상을 아직 안 찍었다. **영상 없이 검증할 수 있는 것은 전부 검증했다.**
영상이 들어오면 `build()` → `shrink()` → `take()` 세 번으로 끝난다.

| 항목 | 상태 |
|---|---|
| colab-mcp 연결 | 됨. 중계기 경유 (아래 "왜 중계기가 필요한가") |
| 붙을 노트 고정 | 실행기로 처리. **다음 재시작부터 적용** |
| 코랩 런타임 | T4 GPU, 모델 적재 완료 |
| 복원 파이프라인 | 합성 영상(`build`)과 **실제 사진(`reconstruct`) 둘 다 검증됨** |
| 줄이기 `shrink()` | 세 형식 변환 검증됨 |
| 뷰어 `my-open-space/index.html` | 브라우저에서 실제로 그려지는 것 확인 |
| 저장소 뼈대 | `index.html`, `README.md`, `.nojekyll`, `shots/` 준비됨 |
| 영상 촬영 | **안 함 — 이것이 유일한 병목** |
| 깃허브 저장소·Pages | 안 함. 공개 행위라 영상 나온 뒤 확인받고 진행 |

## 이 기계 (2026-09-21 확인)

| 항목 | 값 |
|---|---|
| GPU | AMD Radeon 내장뿐. **CUDA 없음 → 복원은 코랩에서만** |
| CPU / 메모리 | 12코어 / 17.8GB |
| OS | Windows 11 → Colab CLI 불가, **Colab MCP 경로** |
| 있는 것 | python 3.12, git, node/npx, uv |
| 없는 것 | ffmpeg / ffprobe (로컬엔 없음. 코랩에는 있어서 지장 없음) |

## 코랩 실측 (2026-09-21, T4 15360MiB, 계산능력 7.5)

| 걸음 | 시간 | 비고 |
|---|---|---|
| map-anything 설치 | 약 25초 | 토치는 안 건드린다 |
| 모델 내려받기 4.91GB | **91.7초** | 커리큘럼 표기 173~194초보다 빠름. 세션 안에서 재실행하면 0초 |
| 프레임 24장 뽑기 | 2.3초 | 1080p 16초 영상 기준 |
| 복원 24장 | **9.9~10.9초** | 최대 GPU **6.74~7.04GB** |

입력 해상도는 16:9 일 때 **518×294** 로 고정된다. 원본이 1080p 여도 마찬가지이고
올릴 수 없다. 가는 것(케이블, 의자 다리)이 뭉개지는 까닭이 여기 있다.

## 형식별 크기 — 355만 알갱이 (2026-09-21, 합성 영상 결과)

| 형식 | 바이트 | 알갱이당 B | 변환 |
|---|---|---|---|
| `.ply` | 241,751,637 | **68.00** | — |
| `.compressed.ply` | 57,883,237 | 16.28 | 20.0초 |
| `.sog` | 23,748,354 | 6.68 | 24.3초 |
| `.spz` | 26,688,784 | 7.51 | 9.0초 |

`.ply` → `.sog` 가 10.2배. 세 변환 모두 알갱이 개수가 원본과 같다.
`.ply` 241.8MB 는 깃허브 파일 한 개 한계 100MiB 를 넘으므로 **줄이기는 건너뛸 수 없다.**

같은 코드를 로컬에서 합성 점 30만 개로 돌린 값도 일치한다:
`.ply` 68.00, `.compressed.ply` 16.28, `.sog` 5.64, `.spz` 7.12.

## 연습 실행 — mip-NeRF 360 garden 24장 (2026-09-22, 실제 사진)

| 항목 | 값 |
|---|---|
| 입력 해상도 | 336×518 (garden 은 3:2 라 16:9 의 518×294 와 다른 칸) |
| 복원 | 12.4초, 최대 GPU 6.99GB |
| 점 (마스크 후 → 걸러낸 뒤) | 3,872,394 → 3,772,060 |
| **카메라 퍼짐비** | **0.289** (0.1 기준선 통과. 잘 찍었을 때의 목표치) |
| `.ply` | 256,500,497 바이트, 알갱이당 68.00 |
| `.sog` | 20,133,164 바이트 (19.2MB), 알갱이당 5.34, 변환 34.2초 |
| 뷰어 | 0.5초에 받아 그림. 반지름 14.93 |

정원의 테이블·벽돌 벽·창·잎사귀가 알아볼 수 있게 나왔다. 합성 입력과 달리
복원 품질을 눈으로 확인한 것은 이것이 처음이다.

로컬 `my-open-space/garden.sog` 로 두었다. `http://localhost:8777/?f=garden.sog` 로 본다.
**연습용이므로 커밋하거나 공개하지 않는다** (CC BY 4.0 이라 출처 표기 없이 쓸 수 없고,
과제 제출물도 아니다).

## 알려진 함정

### `.spz` 는 Spark 로 못 띄운다

splat-transform 3.4.2 와 3.0.0 이 쓰는 `.spz` 는 **판 4** 이고 gzip 없이 생으로 쓴다
(첫 네 바이트가 `NGSP`. 표준대로면 gzip 이라 `1f 8b` 여야 한다).

- Spark 2.1.0 → `Invalid gzip header`
- 직접 gzip 으로 싸서 넣으면 → `Unsupported SPZ version: 4`
- Spark 2.2.0 도 같다. splat-transform 에 판을 고르는 옵션은 없다

**우리 쪽 잘못이 아니다.** 비교표의 `.spz` 칸은 파일 크기와 알갱이 개수로 채우고,
화면에 띄우는 것은 `.sog` 를 쓴다. 뷰어가 조용히 알갱이 0개를 내므로 이걸 모르면
자기 코드 탓으로 착각해 한참 헤맨다.

### `amp_dtype` 기본값이 `bf16` 이다

`model.infer(...)` 의 기본값이 `'bf16'` 인데 T4(계산능력 7.5)에는 bf16 이 없다.
그대로 두면 계산이 32비트로 풀려 메모리가 넘친다.
**8장까지는 bf16 으로도 돌기 때문에 작게 시험하면 통과하고 본 실행에서 터진다.**

### `img_no_norm` 은 0~1 이다

0~255 가 아니다. 구면조화 0차 변환은 `(rgb - 0.5) / 0.28209479177387814`.
`rgb/255` 를 먼저 하면 색이 다 죽는다.

### MCP 도구 호출에 시간 제한이 있다

모델 내려받기(92초)와 형식 변환(20~25초)이 제한에 걸린다.
**백그라운드 스레드로 걸어 두고 상태만 따로 확인한다.** 실제로 그렇게 짜 두었다.

### 왜 중계기가 필요한가

colab-mcp 를 공식 안내대로 등록하면 **셀 조작 도구가 영영 안 보인다.**
연결은 되는데 도구가 `open_colab_browser_connection` 하나뿐이다.

colab-mcp 는 프록시다. 실제 셀 도구는 브라우저 쪽 코랩 페이지가 WebSocket 으로
내려준다. 그런데 클라이언트의 `tools/list` 가 연결보다 **먼저** 도착해 빈 목록을
받아 가고, 연결된 뒤 서버가 보내는 `tools/list_changed` 알림을 이 클라이언트가
처리하지 않아 목록을 다시 안 가져간다.

로그가 증거다. `ListToolsRequest` 뒤 4초 만에 `connection open`, 그 뒤로
`ListToolsRequest` 없음.

`tools/colab_mcp_shim.py` 가 `tools/list` 를 가로채 먼저
`open_colab_browser_connection` 을 주입 호출하고, 연결이 살아난 뒤에 목록을 넘긴다.
게이트가 6.5초 잡고 있다가 통과하면 도구 7개가 다 열린다.

### 중계기를 고칠 때 밟은 지뢰

- 주입하는 JSON-RPC 요청의 `id` 를 **문자열로 주면 자식이 즉시 에러를 돌려준다.** 숫자를 쓴다.
- **응답에 `result` 키가 있는 것과 연결에 성공한 것은 다르다.** 자식은 연결에 실패해도
  정상 응답으로 `{"result": false}` 를 돌려준다. 키 유무로 판정하면 실패를 성공으로
  오해해 게이트가 열리고, 도구 목록이 빈 채로 나간다. `structuredContent.result` 가
  `true` 인지 값을 봐야 한다. 2026-09-22 에 이 버그로 하루 막혔다.
- **크롬이 떠 있는 상태에서 Claude Code 를 시작한다.** 자식이 브라우저에 코랩 탭을
  새로 여는데, 크롬이 꺼져 있으면 찬 시작이라 자식 안쪽 대기 60초를 넘겨 실패한다.
  크롬이 이미 떠 있으면 6.5초면 붙는다.

### 중계기 연결이 실패하면

`tools/shim-debug.log` 를 본다. `gate: connected on attempt N` 이 있으면 정상이고,
`gate: giving up` 이면 브라우저 쪽이 안 붙은 것이다. 크롬을 먼저 띄우고 다시 시작한다.
- `.mcp.json` 에 `uvx git+...` 로 등록하면 매번 git 참조를 새로 확인하느라
  30초 핸드셰이크를 넘겨 `CONNECT_TIMEOUT` 이 난다.
  `uv tool install` 로 깔고 설치된 실행 파일을 직접 가리킨다.

### colab-mcp 는 자기 노트를 연다 — 실행기로 고정했다

colab-mcp 는 `webbrowser.open_new()` 로 **스크래치 노트(`/notebooks/empty.ipynb`)** 를
새 탭에 열어 거기 붙는다. 그 노트는 저장되지 않으니 런타임 유형이 남지 않고,
세션마다 T4 를 손으로 다시 골라야 했다. 기본이 CPU 라
`nvidia-smi: command not found` 가 난다.

`tools/colab_mcp_launch.py` 가 `webbrowser.open_new` 를 가로채, 주소의
프래그먼트(`#mcpProxyToken=...&mcpProxyPort=...`)만 그대로 옮기고 base 를 **Drive 에
저장된 고정 노트**로 바꾼다. Drive 노트는 런타임 유형이 노트에 남으므로 T4 가 유지된다.
노트를 바꾸려면 `COLAB_MCP_NOTEBOOK` 환경변수를 준다.

`colab_mcp` 는 uv 도구 가상환경에만 깔려 있으므로 실행기는 그 가상환경의 파이썬으로
실행해야 한다: `%APPDATA%\uv\tools\colab-mcp\Scripts\python.exe`.

어느 탭인지 헷갈리면 MCP 로 그 노트 맨 위에 표시 셀을 하나 넣어 찾게 하면 된다.

### 코랩 파일을 이 기계로 가져오기

`google.colab.files.download()` 는 MCP 로 셀을 돌려도 동작한다. 조종할 수 없는 탭에서
받아도 크롬이 `~/Downloads` 에 저장하므로 거기서 집어 오면 된다. 19.2MB 가 20초 안에 왔다.

## 안 한 것과 그 이유

| 안 한 것 | 이유 |
|---|---|
| 서드파티 colab-mcp 포크 설치 | 자체 중계기로 해결됨. 남의 코드를 이 기계에서 돌릴 이유가 없어졌다 |
| Spark 2.2.0 으로 올리기 | `.spz` 가 그대로 안 된다. 올릴 이유가 없어 참고 저장소와 같은 2.1.0 을 유지 |
| 로컬 ffmpeg 설치 | 프레임 추출이 코랩에서 돌아 필요 없었다 |
| 깃 저장소 초기화 | 아직 합성 검증 파일뿐이라 커밋하면 쓰레기가 박힌다 |
| 메시 단계 | 과제가 요구하지 않는다. 물리를 붙일 때만 필요 |

## 남은 일

1. **영상 촬영** — 공개 가능 + 시차가 쌓이는 곳. 걸어 다니며 15~20초, 가로, 1080p
2. 코랩에 올리고 `build(경로)` → 카메라 퍼짐비가 0.1 미만이면 다시 찍는다
3. `shrink()` 로 세 형식 + 비교표
4. `take("/content/scene.sog")` 로 내려받아 `my-open-space/scene.sog` 에 둔다
5. `README.md` 의 빈칸을 실측값으로 채운다
6. 깃허브 저장소 만들고 Pages 켜기 — **공개 행위라 먼저 확인받는다**
7. 제출 전 `주소/scene.sog` 가 직접 내려오는지 확인. 페이지는 떠도 파일이 안
   내려오면 화면이 빈다

---

# 사건 기록

## 2026-09-21

- 커리큘럼 전체와 8강 과제 요건을 확인했다. 진도는 3강 수강 중.
- colab-mcp 등록이 세 번 실패했다. `uvx git+` → `CONNECTION_CLOSED`,
  설치 후 실행 파일 직접 지정 → `CONNECT_TIMEOUT`, 중계기 1차(문자열 id) → 연결 안 됨.
  중계기 2차(숫자 id + `result` 확인)에서 도구 7개가 다 열렸다.
- 코랩 T4 에서 모델 적재 91.7초. 합성 이미지 24장으로 복원 경로를 먼저 검증해
  출력 키(`pts3d`, `img_no_norm`, `mask`, `camera_poses`)와 색 범위(0~1)를 확정했다.
  추측으로 코드를 짜지 않으려고 한 것인데, `img_no_norm` 이 0~255 가 아니라는 것을
  여기서 잡았다.
- 합성 영상으로 `build()` 를 끝까지 돌렸다. 카메라 퍼짐비 0.16, 알갱이당 68.00바이트.
- 뷰어를 로컬 서버로 띄워 `.sog` 가 실제로 그려지는 것을 확인했다.
  이때 `.spz` 가 알갱이 0개로 나와 파헤친 끝에 SPZ 판 4 문제를 찾았다.

## 2026-09-22

- 세션을 새로 시작하니 colab-mcp 가 죽어 있었다. 중계기가 연결 실패(`result: false`)를
  성공으로 오해하고 게이트를 연 것이 원인. 응답 **값**을 보도록 고치고 재시도(2회)를
  넣었다. `.mcp.json` 의 `timeout` 은 240000 으로 올렸다.
- 연습용 입력으로 mip-NeRF 360 `garden` 장면(CC BY 4.0)을 쓰기로 했다.
  물체 둘레를 한 바퀴 도는 촬영이라 시차가 최대이고 사람·번호판이 없다.
  HuggingFace `alexmkwizu/gaussian_training_datasets` 의 `mipnerf360/garden/images_4` 에서
  24장만 고르면 몇 MB 다. 영상이 아니라 사진이라 프레임 추출과 번짐 걱정이 없다.
  **연습용이다. 과제 제출물로는 쓸 수 없다** — 과제가 평가하는 것이 장소를 고르는
  판단이고, 결과를 공개 주소에 올리므로 남의 촬영물을 쓸 수 없다.
- garden 24장으로 실제 사진 복원을 끝까지 돌렸다. 실측은 아래 "연습 실행" 표에 있다.
  **카메라 퍼짐비 0.289** 가 나왔는데, 커리큘럼이 "방을 15~20초 걸어 다니며 찍으면
  0.3 언저리" 라고 한 값과 거의 같다. 나가서 찍을 때 목표로 삼을 기준점이다.
- 매 세션 T4 를 손으로 다시 고르는 것을 없애려고 `tools/colab_mcp_launch.py` 를 만들어
  붙을 노트를 Drive 노트로 고정했다. 다음 재시작부터 적용된다.
